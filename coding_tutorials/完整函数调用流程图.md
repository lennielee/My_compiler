# 完整函数调用流程图

## 概述

此图表显示了从C源代码通过所有阶段到可执行RISC-V汇编的完整编译管道，包括组件之间的所有接口调用以及通过整个系统的数据流。

## 1. 完整编译管道流程

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   C源代码       │    │   标记流        │    │   抽象语法树    │    │   带注释的      │
│                 │───▶│   (词法分析器   │───▶│   (解析器输出)  │───▶│   AST           │
│   "int a = b+c" │    │   输出)         │    │                 │    │   (语义分析输出)│
└─────────────────┘    └─────────────────┘    └─────────────────┘    └─────────────────┘
         │                       │                       │                       │
         ▼                       ▼                       ▼                       ▼
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   词法分析器    │    │   解析器        │    │   语义分析器    │    │   代码生成器    │
│   NextToken()   │    │   ParseExpr()   │    │   AnalyzeExpr() │    │   GenerateCode()│
│   LexNumber()   │    │   ParseStmt()   │    │                 │    │                 │
└─────────────────┘    └─────────────────┘    └─────────────────┘    └─────────────────┘
                                                                               │
                                                                               ▼
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   RISC-V        │    │   机器指令      │    │   选择DAG       │    │   LLVM IR       │
│   汇编代码      │◀───│   (后端)        │◀───│   (指令选择)    │◀───│   (平台无关)    │
│   (最终)        │    │                 │    │                 │    │                 │
└─────────────────┘    └─────────────────┘    └─────────────────┘    └─────────────────┘
         ▲                       ▲                       ▲                       │
         │                       │                       │                       ▼
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   汇编打印器    │    │   指令选择      │    │   IR降级        │    │   后端管道      │
│   emitInstruction()│    │   Select()      │    │   LowerCall()   │    │   (LLVM)        │
│                 │    │                 │    │   LowerReturn() │    │                 │
└─────────────────┘    └─────────────────┘    └─────────────────┘    └─────────────────┘
```

## 2. 详细接口调用流程

### 2.1 前端处理链

```cpp
// 主编译驱动程序
int main(int argc, char* argv[]) {
    // 1. 源文件读取
    std::string source = ReadSourceFile(argv[1]);
    
    // 2. 词法分析
    Lexer lexer(llvm::StringRef(source));
    
    // 3. 语法分析  
    Parser parser(lexer);
    auto program = parser.ParseProgram();
    
    // 4. 语义分析
    SemanticAnalyzer analyzer(diagEngine);
    analyzer.AnalyzeProgram(program);
    
    // 5. 代码生成
    CodeGen codegen;
    codegen.GenerateCode(program);
    
    // 6. 后端编译
    CompileToAssembly(program, targetMachine);
}
```

### 2.2 词法分析器 → 解析器接口流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         解析器 ↔ 词法分析器接口                        │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  Parser::ParseExpr() {                                                  │
│    ┌─────────────────┐                                                  │
│    │  currentToken   │──────────────┐                                   │
│    └─────────────────┘              │                                   │
│                                     │                                   │
│    while (...) {                    ▼                                   │
│      ┌─────────────────────────────────────────┐                       │
│      │  if (Expect(TokenType::plus)) {         │                       │
│      │    Advance(); // 调用词法分析器        │                       │
│      │    auto right = ParseTerm();            │                       │
│      │    left = CreateBinaryExpr(op, l, r);   │                       │
│      │  }                                      │                       │
│      └─────────────────────────────────────────┘                       │
│    }                                                                    │
│  }                                                                      │
│                                                                         │
│  void Advance() {                                                       │
│    lexer.NextToken(currentToken); ──────────┐                          │
│  }                                          │                          │
│                                             ▼                          │
│                                    ┌─────────────────┐                  │
│                                    │ Lexer::NextToken│                  │
│                                    │ ├─ SkipWhitespace                  │
│                                    │ ├─ LexNumber                       │
│                                    │ ├─ LexIdentifier                   │
│                                    │ ├─ LexOperator                     │
│                                    │ └─ LexString                       │
│                                    └─────────────────┘                  │
│                                             │                          │
│                                             ▼                          │
│                                    ┌─────────────────┐                  │
│                                    │ Token返回       │                  │
│                                    │ {type, value,   │                  │
│                                    │  location}      │                  │
│                                    └─────────────────┘                  │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.3 解析器 → 语义分析器接口流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    解析器 ↔ 语义分析器接口                              │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  解析器生成AST：                                                        │
│  ┌─────────────────┐                                                    │
│  │ BinaryExpr      │                                                    │
│  │ ├─ left: Expr   │                                                    │
│  │ ├─ op: Add      │                                                    │
│  │ └─ right: Expr  │                                                    │
│  └─────────────────┘                                                    │
│           │                                                             │
│           ▼                                                             │
│  SemanticAnalyzer::AnalyzeExpression(expr) {                           │
│    ┌─────────────────────────────────────────┐                         │
│    │  return expr->Accept(this);             │ ◀── 访问者模式          │
│    └─────────────────────────────────────────┘                         │
│  }                                                                      │
│           │                                                             │
│           ▼                                                             │
│  VisitBinaryExpr(BinaryExpr* expr) {                                   │
│    ┌─────────────────────────────────────────┐                         │
│    │  auto leftType = expr->left->Accept(this);                        │
│    │  auto rightType = expr->right->Accept(this);                      │
│    │                                         │                         │
│    │  if (!IsTypeCompatible(leftType, rightType)) {                    │
│    │    ReportError("Type mismatch");        │                         │
│    │  }                                      │                         │
│    │                                         │                         │
│    │  expr->SetType(DetermineResultType(leftType, rightType));         │
│    │  return expr->GetType();                │                         │
│    └─────────────────────────────────────────┘                         │
│  }                                                                      │
│           │                                                             │
│           ▼                                                             │
│  ┌─────────────────┐                                                    │
│  │ 带注释的AST     │ ◀── AST节点现在有类型信息                        │
│  │ 包含类型信息    │                                                    │
│  └─────────────────┘                                                    │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.4 语义分析器 → 代码生成器接口流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│               语义分析器 ↔ 代码生成器接口                              │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  CodeGen::GenerateCode(annotatedAST) {                                 │
│    ┌─────────────────────────────────────────┐                         │
│    │  InitializeLLVMContext();               │                         │
│    │  CreateMainFunction();                  │                         │
│    │  auto result = annotatedAST->Accept(this);                        │
│    └─────────────────────────────────────────┘                         │
│  }                                                                      │
│           │                                                             │
│           ▼                                                             │
│  VisitBinaryExpr(BinaryExpr* expr) {                                   │
│    ┌─────────────────────────────────────────┐                         │
│    │  // 使用语义分析的类型信息              │                         │
│    │  auto type = expr->GetType(); ◀────────── 来自语义分析            │
│    │  auto llvmType = ConvertToLLVMType(type);                         │
│    │                                         │                         │
│    │  Value* left = expr->left->Accept(this);│                         │
│    │  Value* right = expr->right->Accept(this);                        │
│    │                                         │                         │
│    │  switch (expr->op) {                    │                         │
│    │  case OpCode::Add:                      │                         │
│    │    return builder.CreateAdd(left, right, "add_tmp");              │
│    │  case OpCode::Mul:                      │                         │
│    │    return builder.CreateMul(left, right, "mul_tmp");              │
│    │  }                                      │                         │
│    └─────────────────────────────────────────┘                         │
│  }                                                                      │
│           │                                                             │
│           ▼                                                             │
│  ┌─────────────────┐                                                    │
│  │ LLVM IR         │ ◀── 平台无关的中间代码                            │
│  │ %add_tmp = add  │                                                    │
│  │   i32 %0, %1    │                                                    │
│  └─────────────────┘                                                    │
└─────────────────────────────────────────────────────────────────────────┘
```

## 3. 后端处理链

### 3.1 LLVM IR → SelectionDAG → 机器指令

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        后端处理管道                                     │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  LLVM IR:                                                              │
│  ┌─────────────────┐                                                    │
│  │ %result = add   │                                                    │
│  │   i32 %a, %b    │                                                    │
│  └─────────────────┘                                                    │
│           │                                                             │
│           ▼                                                             │
│  OneISelLowering::LowerOperation() {                                   │
│    ┌─────────────────────────────────────────┐                         │
│    │  // 将LLVM IR节点转换为SelectionDAG     │                         │
│    │  switch (Op.getOpcode()) {              │                         │
│    │  case ISD::ADD:                         │                         │
│    │    // 映射到RISC-V ADD指令              │                         │
│    │    return DAG.getNode(OneISD::ADD, ...);│                         │
│    │  case ISD::GlobalAddress:               │                         │
│    │    return LowerGlobalAddress(Op, DAG);  │                         │
│    │  }                                      │                         │
│    └─────────────────────────────────────────┘                         │
│  }                                                                      │
│           │                                                             │
│           ▼                                                             │
│  ┌─────────────────┐                                                    │
│  │ SelectionDAG    │ ◀── 指令选择的中间表示                            │
│  │ 节点            │                                                    │
│  └─────────────────┘                                                    │
│           │                                                             │
│           ▼                                                             │
│  OneDAGToDAGISel::Select() {                                           │
│    ┌─────────────────────────────────────────┐                         │
│    │  // 首先尝试自动生成的模式              │                         │
│    │  if (SelectCode(Node)) return;          │                         │
│    │                                         │                         │
│    │  // 手动处理特殊情况                   │                         │
│    │  switch (Node->getOpcode()) {           │                         │
│    │  case ISD::Constant:                    │                         │
│    │    // 转换为带零寄存器的ADDI            │                         │
│    │    SDNode *Result = CurDAG->getMachineNode(                       │
│    │        One::ADDI, DL, MVT::i32,         │                         │
│    │        CurDAG->getRegister(One::ZERO, MVT::i32),                  │
│    │        TargetImm);                      │                         │
│    │    ReplaceNode(Node, Result);           │                         │
│    │  }                                      │                         │
│    └─────────────────────────────────────────┘                         │
│  }                                                                      │
│           │                                                             │
│           ▼                                                             │
│  ┌─────────────────┐                                                    │
│  │ 机器指令        │ ◀── 目标特定的机器指令                            │
│  │ ADDI %0, %zero, 5                                                   │
│  └─────────────────┘                                                    │
└─────────────────────────────────────────────────────────────────────────┘
```

### 3.2 函数调用处理流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         函数调用处理                                    │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  C代码: foo(a, b, c)                                                   │
│           │                                                             │
│           ▼                                                             │
│  LLVM IR: call i32 @foo(i32 %a, i32 %b, i32 %c)                       │
│           │                                                             │
│           ▼                                                             │
│  OneTargetLowering::LowerCall() {                                      │
│    ┌─────────────────────────────────────────┐                         │
│    │  // 1. 分析参数                        │                         │
│    │  CCInfo.AnalyzeCallOperands(Outs, CC_One);                        │
│    │                                         │                         │
│    │  // 2. 处理寄存器参数                  │                         │
│    │  for (auto &VA : ArgLocs) {             │                         │
│    │    if (VA.isRegLoc()) {                 │                         │
│    │      RegsToPass.push_back(              │                         │
│    │        {VA.getLocReg(), OutVals[i]});   │                         │
│    │    } else {                             │                         │
│    │      // 存储到栈                       │                         │
│    │      Chain = DAG.getStore(...);         │                         │
│    │    }                                    │                         │
│    │  }                                      │                         │
│    │                                         │                         │
│    │  // 3. 创建调用指令                    │                         │
│    │  Chain = DAG.getNode(OneISD::CALL, ...);│                         │
│    │                                         │                         │
│    │  // 4. 处理返回值
```